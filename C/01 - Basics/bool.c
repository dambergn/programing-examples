#include <stdio.h>
#include <stdbool.h> // Enables the use of True and False for boolean logic

int main(){
  _Bool nickIsSleeping = false; // boolean logic 0 = False 1 = True

  printf("Is Nick Sleeping? (1 is Yes, 0 is No)? %i\n", nickIsSleeping);
  return 0;
}